// lib/core/theme/one_piece_colors.dart
import 'package:flutter/material.dart';

class OnePieceColors {
  static const maroon = Color(0xFF7A2426);      // accent stripe / badges
  static const strawGold = Color(0xFFD9A441);   // primary accent (replaces cyan)
  static const navyInk = Color(0xFF1E3558);     // secondary
  static const boneWhite = Color(0xFFF4F1E8);   // text
  static const deepBackground = Color(0xFF050509); // main dark bg
  static const surfaceDark = Color(0xFF101017);    // cards, app bars
}
